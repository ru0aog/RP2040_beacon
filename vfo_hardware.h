/**  
 * ============================================================================  
 *  vfo_hardware.h — Публичный интерфейс и конфигурация программного VFO  
 *  Версия 2.09 от 2026-09-25, автор RU0AOG  
 * ============================================================================  
 *  
 *  Заголовок объявляет Low-Level API аварийного ВЧ-генератора на PIO RP2040  
 *  (GPIO 28) и содержит ВСЕ компиляционные переключатели поведения модуля.  
 *  Реализация — в vfo_hardware.cpp.  
 *  
 *  КОНФИГУРАЦИЯ (закомментируйте/раскомментируйте нужные #define):  
 *    VFO_USE_MASH2          — MASH-1-1 (дельта-сигма 2-го порядка) вместо 1-го.  
 *    VFO_DITHER_RANDOMIZE   — рандомизация входа аккумулятора (Xorshift32).  
 *    VFO_DITHER_RAND_BITS   — амплитуда рандомизации в младших битах (4 = -7..+7).  
 *    VFO_SNAP_TO_GRID       — привязка частоты в окне +-0.1 Гц (минимизация dds_step).  
 *    VFO_DITHER_ON_CORE1    — вынос дизеринга в плотный цикл на Core 1;  
 *                             если выключено — таймер на Core 0.  
 *    VFO_CLOCK_133_MHZ      — разгон clk_sys до 133 МГц; иначе 120 МГц.  
 *    VFO_DITHER_INTERVAL_US — интервал таймера (только для режима Core 0),  
 *                             10 мкс = 100 кГц, 5 мкс = 200 кГц.  
 *  
 *  Чтобы услышать «чистый» старый вариант — закомментируйте VFO_USE_MASH2 и  
 *  VFO_DITHER_RANDOMIZE (спуры пойдут ровно через частоту обновления дизеринга).  
 *  
 *  АППАРАТНАЯ ЧАСТЬ:  
 *    VFO_OUTPUT_PIN         — вывод сигнала (GPIO 28).  
 *    VFO_IFKP_TONES_COUNT   — размер предрассчитанной сетки тонов (33).  
 *    VFO_CALIBRATED_XOSC_HZ — истинная частота кварца конкретной платы (калибровка).  
 *  
 *  struct VfoParameters — предрассчитанные коэффициенты одного тона: целая/дробная  
 *    части делителя PIO (pio_int/pio_frac), шаг DDS-аккумулятора (dds_step) и  
 *    целевая частота в сантигерцах (target_freq_chz).  
 * ============================================================================  
 */

#ifndef VFO_HARDWARE_H
#define VFO_HARDWARE_H

#include <Arduino.h>

// === Конфигурация механизмов снижения спуров ===
#define VFO_USE_MASH2            // Включить Delta-Sigma 2-го порядка (MASH-1-1). Если выключено — 1-й порядок.
#define VFO_DITHER_RANDOMIZE     // Включить рандомизацию входа аккумулятора (Dither Injection)
#define VFO_DITHER_RAND_BITS   4 // Амплитуда рандомизации в младших битах (4 бита: шум в диапазоне -7..+7)
#define VFO_SNAP_TO_GRID         // Включить привязку частоты в окне +-0.1 Гц для минимизации dds_step

// === ЧАСТЬ A: Двухъядерный режим ===
#define VFO_DITHER_ON_CORE1      // Вынос дизеринга в плотный цикл на Core 1.
                                 // Если закомментировано — дизеринг работает на Core 0 по таймеру.

// === ЧАСТЬ B: Тактовая частота ===
#define VFO_CLOCK_133_MHZ        // Разгон clk_sys до 133 МГц.
                                 // Если закомментировано — тактовая частота фиксируется на 120 МГц.

// === Настройка интервала таймера (используется только если VFO_DITHER_ON_CORE1 выключен) ===
#define VFO_DITHER_INTERVAL_US 10 

// === Аппаратная конфигурация физического уровня ===
#define VFO_OUTPUT_PIN       28            // Сигнал строго на GPIO 28
#define VFO_IFKP_TONES_COUNT 33            // Количество фиксированных тонов в сетке
#define VFO_TONE_NONE        255           // Флаг неопределенного тона

// Истинная физическая частота опорного кварца вашего экземпляра платы (калибровка)
#define VFO_CALIBRATED_XOSC_HZ 12000350ULL

// Структура параметров частоты для низкоуровневых регистров PIO и таймера фазы
struct VfoParameters {
    uint32_t pio_int;
    uint32_t pio_frac;
    uint32_t dds_step;
    uint32_t target_freq_chz; // Частота в сантигерцах (0.01 Гц)
};

extern VfoParameters ifkp_tones[VFO_IFKP_TONES_COUNT];

// === НАБОР ФУНКЦИЙ УПРАВЛЕНИЯ ВЧ-ЭФИРОМ (Low-Level API) ===
void vfo_hardware_init(unsigned int base_freq_hz, double step_hz);
void vfo_set_tone_instant(uint8_t tone_index);
void vfo_set_cw_key(bool key_down);

#endif // VFO_HARDWARE_H
