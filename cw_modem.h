/**  
 * ============================================================================  
 *  cw_modem.h — Публичный интерфейс передатчика CW (код Морзе)  
 * ============================================================================  
 *  
 *  Объявляет API телеграфного модулятора и глобальную настройку скорости.  
 *  
 *  ГЛОБАЛЬНЫЕ ПЕРЕМЕННЫЕ:  
 *    CW_DOT_TIME_MS — длина точки в миллисекундах (задаёт скорость передачи;  
 *        определяется в основном скетче, пересчитывается из WPM в file_manager).  
 *        Не рекомендуется задавать меньше 20 мс.  
 *  
 *  API:  
 *    prepare_cw_frequency(freq_hz)          — установка частоты несущей CW.  
 *    send_cw_string(const char*) / (String) — передача текста кодом Морзе  
 *        (перегрузки для C-строк и объектов String).  
 * ============================================================================  
 */

#ifndef CW_MODEM_H
#define CW_MODEM_H

#include <Arduino.h>

// Настройка скорости CW (длина точки в миллисекундах)
// меньше 20 не стоит делать
extern uint32_t CW_DOT_TIME_MS; 

void prepare_cw_frequency(uint32_t freq_hz);
void send_cw_string(const char* str);
void send_cw_string(String str);

#endif
